App = Em.Application.create({
    rootElement: $(".view-container")
}), App.Router.map(function() {
    this.route("subjects", {
        path: "/subjects"
    }), this.route("insert", {
        path: "/insert"
    });
}), App.IndexRoute = Ember.Route.extend({
    setupController: function(a) {
        a.set("model", [ "a", "b", "c" ]);
    }
}), App.Subject = DS.Model.extend({
    name: DS.attr("string"),
    age: DS.attr("int")
}), App.SubjectsRoute = Ember.Route.extend({
    model: function() {
        return this.store.find("subject");
    }
}), App.InsertRoute = Ember.Route.extend({
    model: function() {
        return App.MongoSample.one();
    },
    setupController: function(a, b) {
        a.set("model", b);
    }
}), App.InsertController = Ember.ObjectController.extend({
    actions: {
        insert_user: function() {
            App.MongoSample.save(this.get("model"));
        }
    }
}), App.SubjectController = Ember.ObjectController.extend({
    actions: {
        removeSubject: function() {
            console.log("calling deleteSubject method...");
            var a = this.get("model");
            $.ajax({
                url: "http://localhost:8082/api/subjects/" + a.id,
                type: "DELETE",
                contentType: "text/plain",
                success: function(b) {
                    $("#" + a.id).remove(), alert("Result: " + b);
                }
            });
        }
    }
}), App.ShellRestAdapter = DS.RESTAdapter.extend({
    pathForType: function(a) {
        return a;
    },
    find: function(a, b, c, d) {
        return this.ajax(this.buildURL(b.typeKey + "/get", c, d), "GET");
    },
    findAll: function(a, b, c) {
        var d;
        return c && (d = {
            since: c
        }), this.ajax(this.buildURL(b.typeKey + "/list"), "GET", {
            data: d
        });
    }
}), App.ApplicationAdapter = DS.RESTAdapter.extend({
    host: "http://localhost:8082/api"
}), App.Store = DS.Store.extend({});