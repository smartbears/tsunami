App = Em.Application.create({
    rootElement: $(".view-container")
}), App.Router.map(function() {
    this.route("mongo", {
        path: "/mongo"
    });
}), App.IndexRoute = Ember.Route.extend({
    setupController: function(a) {
        a.set("model", [ "a", "b", "c" ]);
    }
}), App.MongoSample = Ember.Object.extend(), App.MongoSample.reopenClass({
    all: function() {
        return $.getJSON("http://localhost:8081/api/home").then(function(a) {
            var b = [];
            return a.forEach(function(a) {
                b.push(App.MongoSample.create(a));
            }), console.log(b), b;
        });
    }
}), App.MongoRoute = Ember.Route.extend({
    model: function() {
        return App.MongoSample.all();
    }
}), $.postJSON = function(a, b, c, d) {
    return d = $.extend({
        url: a,
        type: "POST",
        data: JSON.stringify(b),
        contentType: "application/json; charset=utf-8",
        dataType: "json",
        async: !0,
        success: c
    }, d), $.ajax(d);
};